PORT=3000
NODE_ENV=development

# PostgreSQL connection string (required)
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/bridgecommander

# Auto-create tables on boot (dev only)
DB_SYNC=true

# Print SQL statements
LOG_SQL=false
